@model Product

<div class="border-2 border-black p-3 rounded">
    <h3 class="float-right rounded-full bg-blue-800 text-white px-2">@Model?.Price.ToString("c")</h4>
    <h2 class="text-xl">@Model?.Name</h3>
    <p>@Model?.Description</p>
</div>